# Canonical Pati–Salam interval EFT: next calculation specification

This is a **task specification and pass checklist**, not a completed EFT derivation or an admission of the lower one-loop scalar match. The existing `LOWER_BOUNDARY_MATCHING_BLOCKED`, `PS_THRESHOLD_KERNEL_BLOCKED`, `GAUGE_MATCHING_BLOCKED`, and `BFB_UNRESOLVED` dispositions remain unchanged.

## Frozen task for Codex

> Pause the lower one-loop gauge match. From [`PARENT_ACTION_V1`](../calculations/canonical_so10_scalar_reconstruction/PARENT_ACTION_V1.md), the frozen [positive-Higgs point](../calculations/canonical_so10_positive_higgs/POINT.json), and the [exact PS projectors](../calculations/canonical_so10_ps_threshold_kernel/certify_ps_projectors.py), construct a tree-level, power-counted Pati–Salam interval EFT for `Spin(10) -> PS × D -> SM`. Keep the published Babu–Khan scalar formulas out of the derivation. First identify a stationary PS-side background of the **same frozen theory** (or prove why a background-field expansion about another configuration is controlled); do not use the existing nonstationary `sigma -> 0` spectrum as physical threshold masses. Specify a scaling family and remainder estimate for `epsilon = sigma/omega`, rather than inferring parametric decoupling from its benchmark value `0.1` alone. Decompose the complete parent fields, gauge generators, kinetic terms, and interactions with the certified PS projectors. Classify each PS-covariant multiplet and any mixing as upper-heavy, interval-retained, or unresolved by the quadratic operators and their epsilon expansion, with an explicit once-only field ledger. Integrate upper-heavy fields at tree level using covariant equations of motion/Schur complements, and give the renormalizable PS action plus the leading induced higher-dimensional operators, including the 24 upper-vector current–current operators. Match their coefficients and remainder to the frozen parent normalization; test PS covariance, the intended D action, gauge/Goldstone bookkeeping, and symmetry-restoration limits. Bound which retained masses and induced operators can affect the lower-boundary one-loop gauge-kinetic `F²` match at the claimed accuracy. Treat the previously proposed all-scalars-delayed-decoupling assignment only as a comparator. Return `PS_INTERVAL_EFT_DEFINED`, `PS_INTERVAL_EFT_POWER_COUNTING_FAIL`, or `PS_INTERVAL_EFT_BLOCKED`, with precise field and operator causes. Do **not** resume the lower one-loop scalar subtraction, begin the upper one-loop match, fit scales, or perform flavor/proton-decay work before a pass.

The calculation must begin with the Charter's twelve-field record. Its authority ceiling is a **tree-level interval field/operator definition and quantified truncation**, not one-loop threshold matching or phenomenological viability.

## Pass checklist

| Gate | Required evidence | Stop/fail condition |
| --- | --- | --- |
| Parent identity | Verify the frozen parent-action and point hashes; use one tensor, kinetic, generator and PQ convention throughout. | An inherited published scalar formula or changed benchmark silently enters. |
| PS-side background | Solve the action-derived stationarity equations for the intended PS phase with `sigma = 0` where appropriate, or specify and justify a controlled off-shell background-field expansion. Distinguish an instability toward the lower breaking from a failed high-scale definition. | The nonstationary restoration spectrum is used as physical threshold masses. |
| Scaling/power counting | State how `omega`, `sigma`, `v`, dimensionful couplings, and tuned dimensionless couplings scale in a declared epsilon family; provide an error estimate at `epsilon = 0.1`. Use mass **squared** orders such as `omega²`, `omega sigma`, and `sigma²` consistently. | A numerical small mass at one point is called parametrically light without a scaling argument, or the remainder is uncontrolled. |
| Complete PS decomposition | Project every scalar and gauge degree of freedom, with canonical kinetic metrics; verify `54_R = 1+20'+9+24`, `126_C = 6+60+30+30`, `10_C = 6+4`, and gauge `45 = 21+24`, including real/complex multiplicities. | A representation or state is missing, double-counted, or assigned from a broken-phase eigenvector alone. |
| Quadratic split | Derive PS-covariant mass and mixing operators from the parent action, including Goldstones; identify a uniformly invertible upper-heavy block and bounded heavy–light mixing. Show the Schur complement and compare its expansion to the exact projected operator. | A retained field has an unsuppressed upper-scale mass/mixing that invalidates the declared expansion, or a required heavy inverse is singular. |
| Tree-level action | Match all retained renormalizable PS terms from the parent kinetic and interaction terms; solve upper-heavy equations of motion covariantly and list the leading local operators, coefficients, dimensions and symmetries, including upper-vector `J_A^mu (M_V^{-2})_{AB} J_{B mu}` with its derived sign/normalization. | Only a spectrum partition is supplied, with no field/operator matching. |
| Symmetry and ledger | Verify exact PS gauge covariance, the claimed D-parity implementation, PQ selection rules, first-stage 24 eaten directions, and once-only placement of all fields into upper matching, PS running, or lower matching. | A field is retained and integrated out twice, or an induced term breaks a required symmetry without a spurion explanation. |
| Lower `F²` relevance | List all induced operators that can enter lower one-loop gauge matching by insertion or vev; bound effects in epsilon and loop order. In particular, justify whether dimension-six current–current and gauge-kinetic operators are negligible, retained, or require matching. | A formally `1/M_U²` term is discarded while a retained loop mass is `O(M_U)` or its contribution is otherwise unsuppressed. |
| Limits/replay | Reproduce exact PS projector ranks and vector hierarchy; in symmetry-restoring limits check multiplet recombination as a representation test, not as physical masses. Independently replay representative Schur coefficients and field counts. | Failure of covariance, recombination, or independent bookkeeping. |

`PS_INTERVAL_EFT_DEFINED` requires **every** gate, including a definite retained-field list and an accuracy statement. `PS_INTERVAL_EFT_POWER_COUNTING_FAIL` means the declared two-scale truncation is demonstrably uncontrolled for this frozen benchmark; it is **not** a no-go for the parent action or every possible EFT scheme. `PS_INTERVAL_EFT_BLOCKED` means a required term, convention, scaling family, or error bound remains uncomputed or ambiguous. A failed delayed-decoupling comparator alone does not establish failure of a conventional decoupling EFT.

After a pass, the first authorized continuation is the **lower** scalar UV/PS-EFT hard-region subtraction combined with the already earned vector term `lambda_a^V = T_a[1-21 log(M_charged/mu)]`, `T=(14/5,0,1)`. The complete lower beta-jump and matching-scale derivative must then be rechecked in the newly defined interval theory. Neither the earlier delayed-decoupling logarithmic checksum nor this tree-level EFT gate alone earns `LOWER_BOUNDARY_MATCHING_PASS`.
